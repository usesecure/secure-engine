# Phase 6.10 frozen CMS Nova handoff

This directory records only the permitted aggregate provenance used to reproduce the Phase 6.10
authorization false-positive classes. It contains no CMS Nova source, finding-level paths, case
identifiers, scanner exceptions, or benchmark material. The independently authored Engine tests
use unrelated names and structures.

The source and evaluation artifacts remain external and read-only. CMS Nova is scanned exactly
once after the implementation and independent regression corpus are frozen.
