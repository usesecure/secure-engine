# Phase 6.15 tranche 4 synthetic fixtures

The vulnerable fixture sends a structurally secret-named environment value to logging and a model
provider. The control logs explicit redacted metadata and sends only its prompt. These are
independent synthetic examples.
