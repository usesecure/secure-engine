# Phase 6.15 tranche 3 synthetic fixtures

The vulnerable fixture exposes dynamic CLI option position, dynamic SQL structure, and a merge
into a shared prototype. The control uses an option delimiter, bound SQL data, and a null-prototype
destination. These are independent synthetic examples.
