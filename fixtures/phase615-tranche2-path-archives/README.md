# Phase 6.15 tranche 2 synthetic fixtures

The vulnerable fixture composes an archive-controlled member path into a filesystem write. The
control writes each member under a generated local name. Both examples are independent synthetic
code.
