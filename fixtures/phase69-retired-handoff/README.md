# Phase 6.9 retired diagnostic handoff

This directory records only aggregate, disclosed Phase 15 diagnostic facts needed to prove the
Phase 6.9 remediation boundary. It contains no Secure Bench fixture source, expectation document,
raw Phase 14 report, evaluator implementation, case-specific alias, or benchmark result repair.

The seven input hashes and aggregate counts in `summary.json` were independently verified before
implementation. The retired handoff is development evidence, not a hidden holdout and not a
prospective score. Scanner-independent adapter projection loss remains outside Secure Engine.
