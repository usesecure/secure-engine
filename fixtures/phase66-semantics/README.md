# Phase 6.6 generated independent scenarios

The executable scenario definitions live in `crates/secure-engine/tests/evidence_semantics_phase66.rs`. Tests materialize each source set into a fresh temporary repository so scenarios cannot influence one another. See `docs/independent-fixture-methodology-phase66.md` for coverage and isolation rules.
