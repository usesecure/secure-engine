# Rules

No vulnerability rules are implemented in Phase 0. This directory is reserved for versioned deterministic rules only after the `secure-json-v1` inventory contract and evidence model are stable and tested. See `PLAN.md` Phase 3.
