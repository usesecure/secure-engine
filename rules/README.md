# Rules

No vulnerability rules are implemented in Phase 2. This directory is reserved for versioned deterministic rules only after the `secure-json-v1` inventory, parsing, and normalized-fact contracts are stable and tested. See `PLAN.md` Phase 3.
